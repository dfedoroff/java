package main.notes.infrastructure.persistance;

/**
 * Интерфейс для конфигурации модели, предоставляющий шаблон для создания конфигураций маппинга объектов
 * определённого типа к структуре таблицы базы данных.
 *
 * @param <T> тип объекта, для которого создаётся конфигурация
 */
public interface ModelConfiguration<T> {
    // В данный момент интерфейс не содержит методов, но служит для определения типа конфигурации модели.
}
